export const DASHBOARD_API_URL =
    "https://api.coingecko.com/api/v3/coins/markets?vs_currency=usd&order=market_cap_desc&per_page=100&page=1&sparkline=false";

export const COMPARE_API_URL = "";

export const COIN_API_URL = "";

//Here we store all apis so that if we want to edit api we can change here so that we can edit in all page

// success response of api - 200 to 299